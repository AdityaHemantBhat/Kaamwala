diff --git a/node_modules/react-native-cashfree-pg-sdk/android/build.gradle b/node_modules/react-native-cashfree-pg-sdk/android/build.gradle
index 826f4c4..9d69447 100644
--- a/node_modules/react-native-cashfree-pg-sdk/android/build.gradle
+++ b/node_modules/react-native-cashfree-pg-sdk/android/build.gradle
@@ -13,11 +13,15 @@ buildscript {
 
 apply plugin: 'com.android.library'
 
+def safeExtGet(prop, fallback) {
+    rootProject.ext.has(prop) ? rootProject.ext.get(prop) : fallback
+}
+
 android {
   namespace "com.reactnativecashfreepgsdk"
+    compileSdk safeExtGet("compileSdkVersion", 36)
     defaultConfig {
-      minSdkVersion 21
-      compileSdk 35
+      minSdkVersion safeExtGet("minSdkVersion", 21)
     }
 
     buildTypes {
@@ -34,23 +38,6 @@ android {
     }
 }
 
-repositories {
-    mavenLocal()
-    maven {
-        // All of React Native (JS, Obj-C sources, Android binaries) is installed from npm
-        url("$rootDir/../node_modules/react-native/android")
-    }
-    google()
-    mavenCentral()
-}
-
-rootProject.allprojects {
-  repositories {
-    google()
-    mavenCentral()
-  }
-}
-
 dependencies {
   //noinspection GradleDynamicVersion
   implementation 'androidx.appcompat:appcompat:1.3.0'
@@ -59,5 +46,5 @@ dependencies {
   implementation 'androidx.gridlayout:gridlayout:1.0.0'
 
   implementation 'com.cashfree.pg:api:2.4.0'
-  implementation "com.facebook.react:react-native:+"  // From node_modules
+  implementation "com.facebook.react:react-native:+"
 }
diff --git a/node_modules/react-native-cashfree-pg-sdk/lib/commonjs/index.js b/node_modules/react-native-cashfree-pg-sdk/lib/commonjs/index.js
index 3971fee..21f0bf1 100644
--- a/node_modules/react-native-cashfree-pg-sdk/lib/commonjs/index.js
+++ b/node_modules/react-native-cashfree-pg-sdk/lib/commonjs/index.js
@@ -55,7 +55,6 @@ class CFPaymentGateway {
     return new Promise((resolve, reject) => {
       if (_reactNative.Platform.OS === 'ios') {
         let fetchUpiList = apps => {
-          console.log(JSON.stringify(apps));
           if (apps) {
             resolve(apps);
           } else {
@@ -85,7 +84,6 @@ class CFPaymentGateway {
     } else if (cfPayment instanceof _cashfreePgApiContract.CFNBPayment) {
       CashfreePgApi.doElementNBPayment(paymentData);
     } else {
-      console.log('makePayment::==> Wrong payment object');
     }
   }
   makeSubsPayment(cfPayment) {
@@ -98,12 +96,10 @@ class CFPaymentGateway {
     } else if (cfPayment instanceof _cashfreePgApiContract.CFSubsNBPayment) {
       CashfreePgApi.doSubsNBPayment(paymentData);
     } else {
-      console.log('makePayment::==> Wrong payment object');
     }
   }
   setEventSubscriber(cfEventCallback) {
     let eventFunction = event => {
-      console.log(JSON.stringify(event));
       let data = JSON.parse(event);
       cfEventCallback.onReceivedEvent(data.eventName, data.meta);
     };
@@ -120,11 +116,9 @@ class CFPaymentGateway {
   setCallback(cfCallback) {
     // this.cfCallback = cfCallback;
     let successFunction = orderID => {
-      console.log('response is : ' + JSON.stringify(orderID));
       cfCallback.onVerify(orderID);
     };
     let failureFunction = error => {
-      console.log('reason: ' + JSON.stringify(error));
       const response = new CFErrorResponse();
       // @ts-ignore
       const message = JSON.parse(error);
@@ -159,9 +153,7 @@ class CFErrorResponse {
     _defineProperty(this, "type", 'request_failed');
   }
   fromJSON(errorString) {
-    console.log('errorString :' + errorString);
     const object = JSON.parse(errorString);
-    console.log('errorStringObject :' + object);
     this.status = object.status;
     this.message = object.message;
     this.code = object.code;
diff --git a/node_modules/react-native-cashfree-pg-sdk/lib/module/index.js b/node_modules/react-native-cashfree-pg-sdk/lib/module/index.js
index 57a2606..44a0dde 100644
--- a/node_modules/react-native-cashfree-pg-sdk/lib/module/index.js
+++ b/node_modules/react-native-cashfree-pg-sdk/lib/module/index.js
@@ -48,7 +48,6 @@ class CFPaymentGateway {
     return new Promise((resolve, reject) => {
       if (Platform.OS === 'ios') {
         let fetchUpiList = apps => {
-          console.log(JSON.stringify(apps));
           if (apps) {
             resolve(apps);
           } else {
@@ -78,7 +77,6 @@ class CFPaymentGateway {
     } else if (cfPayment instanceof CFNBPayment) {
       CashfreePgApi.doElementNBPayment(paymentData);
     } else {
-      console.log('makePayment::==> Wrong payment object');
     }
   }
   makeSubsPayment(cfPayment) {
@@ -91,12 +89,10 @@ class CFPaymentGateway {
     } else if (cfPayment instanceof CFSubsNBPayment) {
       CashfreePgApi.doSubsNBPayment(paymentData);
     } else {
-      console.log('makePayment::==> Wrong payment object');
     }
   }
   setEventSubscriber(cfEventCallback) {
     let eventFunction = event => {
-      console.log(JSON.stringify(event));
       let data = JSON.parse(event);
       cfEventCallback.onReceivedEvent(data.eventName, data.meta);
     };
@@ -113,11 +109,9 @@ class CFPaymentGateway {
   setCallback(cfCallback) {
     // this.cfCallback = cfCallback;
     let successFunction = orderID => {
-      console.log('response is : ' + JSON.stringify(orderID));
       cfCallback.onVerify(orderID);
     };
     let failureFunction = error => {
-      console.log('reason: ' + JSON.stringify(error));
       const response = new CFErrorResponse();
       // @ts-ignore
       const message = JSON.parse(error);
@@ -152,9 +146,7 @@ export class CFErrorResponse {
     _defineProperty(this, "type", 'request_failed');
   }
   fromJSON(errorString) {
-    console.log('errorString :' + errorString);
     const object = JSON.parse(errorString);
-    console.log('errorStringObject :' + object);
     this.status = object.status;
     this.message = object.message;
     this.code = object.code;
diff --git a/node_modules/react-native-cashfree-pg-sdk/src/index.js b/node_modules/react-native-cashfree-pg-sdk/src/index.js
index 932f74f..7787fcb 100644
--- a/node_modules/react-native-cashfree-pg-sdk/src/index.js
+++ b/node_modules/react-native-cashfree-pg-sdk/src/index.js
@@ -50,7 +50,6 @@ class CFPaymentGateway {
         return new Promise((resolve, reject) => {
             if (Platform.OS === 'ios') {
                 let fetchUpiList = (apps) => {
-                    console.log(JSON.stringify(apps));
                     if (apps) {
                         resolve(apps);
                     }
@@ -86,7 +85,6 @@ class CFPaymentGateway {
             CashfreePgApi.doElementNBPayment(paymentData);
         }
         else {
-            console.log('makePayment::==> Wrong payment object');
         }
     }
     makeSubsPayment(cfPayment) {
@@ -102,12 +100,10 @@ class CFPaymentGateway {
             CashfreePgApi.doSubsNBPayment(paymentData);
         }
         else {
-            console.log('makePayment::==> Wrong payment object');
         }
     }
     setEventSubscriber(cfEventCallback) {
         let eventFunction = (event) => {
-            console.log(JSON.stringify(event));
             let data = JSON.parse(event);
             cfEventCallback.onReceivedEvent(data.eventName, data.meta);
         };
@@ -125,11 +121,9 @@ class CFPaymentGateway {
     setCallback(cfCallback) {
         // this.cfCallback = cfCallback;
         let successFunction = (orderID) => {
-            console.log('response is : ' + JSON.stringify(orderID));
             cfCallback.onVerify(orderID);
         };
         let failureFunction = (error) => {
-            console.log('reason: ' + JSON.stringify(error));
             const response = new CFErrorResponse();
             // @ts-ignore
             const message = JSON.parse(error);
@@ -165,9 +159,7 @@ export class CFErrorResponse {
     code = 'payment_failed';
     type = 'request_failed';
     fromJSON(errorString) {
-        console.log('errorString :' + errorString);
         const object = JSON.parse(errorString);
-        console.log('errorStringObject :' + object);
         this.status = object.status;
         this.message = object.message;
         this.code = object.code;
